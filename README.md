# is02p02-mrl-benchmarker
"Most embedding models produce one fixed-size vector — you use all 1536 dimensions or none. MRL changes this: the first 256 dimensions of a 1536-dim MRL embedding are themselves a valid high-quality embedding. Train once. Truncate anywhere. Benchmark to find the smallest dimension that meets your recall target."
